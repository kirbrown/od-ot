<%= title "Add Item to #{@todo_list.title}" %>

<%= form_for [@todo_list, @todo_item], builder: FoundationFormBuilder do |form| %>
  <%= render 'form', form: form %>

  <%= form.submit 'Save', class: 'button large centered' %>

<% end %>
